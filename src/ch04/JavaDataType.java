package ch04;

public class JavaDataType {
    public static void main(String[] args) {
        // 참조형 선언(자바 9 버전부터는 사용되지 않음)
        Integer intRef = new Integer(7);
        // 문자형으로 변환하여 출력
        System.out.println(intRef.toString());
    }
}
